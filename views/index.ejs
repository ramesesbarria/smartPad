<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="app-shell">
    <header class="app-header">
      <div class="brand">
        <span class="brand-badge">SP</span>
        <div class="brand-text">
          <h1>SmartPad</h1>
          <p class="subtitle">Lightweight pastebin-style notes with shareable codes</p>
        </div>
      </div>
    </header>

    <main class="app-main">
      <!-- Left: Quick Save -->
      <section class="card">
        <h2>Quick Save</h2>
        <p class="card-description">
          Create a disposable pad that expires after a time you choose.
          Share the link or pad code with anyone.
        </p>

        <form action="/quick-save" method="POST" class="pad-form">
          <label>
            Title (optional)
            <input type="text" name="title" placeholder="My note">
          </label>

          <label>
            Content
            <textarea name="content" rows="10" placeholder="Type or paste your text here..." required></textarea>
          </label>

          <label>
            Time to live (TTL)
            <select name="ttlMinutes">
              <option value="15">15 minutes</option>
              <option value="60" selected>1 hour</option>
              <option value="1440">1 day</option>
              <option value="10080">7 days</option>
            </select>
          </label>

          <button type="submit" class="primary-btn">Save pad</button>
        </form>
      </section>

      <!-- Right: Open existing pad -->
      <section class="card secondary-card">
        <h2>Open pad</h2>
        <p class="card-description">
          Already have a pad code or link? Enter a code below to view it.
        </p>

        <form action="/open" method="POST" class="open-form">
          <label>
            Pad code
            <input type="text" name="code" placeholder="e.g. ABC123" required>
          </label>
          <button type="submit" class="secondary-btn full-width-btn">Open</button>
        </form>

        <hr class="card-divider">

        <h3 class="mini-heading">How it works</h3>
        <ul class="feature-list">
          <li>✍️ Paste or type any text into Quick Save</li>
          <li>⏱ Choose how long the pad should stay alive</li>
          <li>🔗 Share the URL or pad code with others</li>
          <li>🧹 After it expires, the pad is automatically removed</li>
        </ul>
      </section>
    </main>
  </div>
</body>
</html>
