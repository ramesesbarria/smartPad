<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %> · SmartPad</title>
  <style>
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: #020617;
      color: #e5e7eb;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }
    .card {
      background: #020617;
      border: 1px solid #1f2937;
      padding: 20px 24px;
      border-radius: 18px;
      max-width: 360px;
      width: 100%;
      box-shadow: 0 25px 60px rgba(0, 0, 0, 0.8);
    }
    h1 {
      font-size: 1.3rem;
      margin-bottom: 4px;
    }
    p {
      font-size: 0.9rem;
      color: #9ca3af;
      margin-bottom: 10px;
    }
    .error {
      color: #fca5a5;
      margin-bottom: 10px;
      font-size: 0.85rem;
    }
    label {
      display: block;
      font-size: 0.85rem;
      margin-bottom: 6px;
    }
    input[type="password"] {
      width: 100%;
      padding: 6px 8px;
      border-radius: 10px;
      border: 1px solid #1f2937;
      background: #020617;
      color: #e5e7eb;
      margin-bottom: 10px;
    }
    button {
      border-radius: 999px;
      border: 1px solid #38bdf8;
      background: #38bdf8;
      color: #020617;
      padding: 6px 12px;
      font-size: 0.9rem;
      cursor: pointer;
      font-weight: 600;
    }
  </style>
</head>
<body>
  <div class="card">
    <h1>SmartPad</h1>
    <p>Pad <code><%= code %></code> is password-protected.</p>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form method="GET" action="/pad/<%= code %>">
      <label for="password">Password</label>
      <input id="password" name="password" type="password" autofocus required>
      <button type="submit">Open pad</button>
    </form>
  </div>
</body>
</html>
